Request rate by endpoint:
sum by (endpoint) (rate(capstone_requests_total[5m]))
Output: capstone_requests_total{endpoint="/orders"} 0.2

Error rate percentage:
100 * sum(rate(capstone_requests_total{status=~"4..|5.."}[5m])) / sum(rate(capstone_requests_total[5m]))
Output: 4.0

95th percentile latency:
histogram_quantile(0.95, sum by (le, endpoint) (rate(capstone_request_seconds_bucket[5m])))
Output: 0.012

An in-flight gauge is correct because it describes the current level; a counter only increases and cannot represent requests finishing. When the API stops, rate() treats the counter as having no new samples and the observed rate falls to zero; an error-rate alert does not fire because there are no requests producing errors.